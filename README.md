# TellRidhay — flat upload version

All files are at the root so GitHub's file picker can upload everything.
After uploading, RENAME build-apk.yml to `.github/workflows/build-apk.yml` (see steps given with this zip).
